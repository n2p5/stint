{
  "name": "stint-example-basic",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"pnpm --dir ../.. dev\" \"vite\"",
    "dev:fast": "vite",
    "build": "pnpm --dir ../.. build && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "stint": "workspace:*"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.8.3",
    "vite": "^5.0.0"
  }
}